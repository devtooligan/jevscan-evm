# Reference only. The application never loads .env automatically.
# Export credentials into your process environment; never commit filled values.
MAINNET_RPC_URL=
ETHERSCAN_API_KEY=
TYPESAFE_API_KEY=
# Dedicated owner-controlled directory for caches, mixer data and spending ledger.
JEVSCAN_DATA_DIR=
