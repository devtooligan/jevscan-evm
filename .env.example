# Copy to .env and fill in. Variables already set in the environment win.

# Required for scans. Get a key at https://console.typesafe.ai
TYPESAFE_API_KEY=

# Only for sources/sync.py and sources/build_detector_map.py (regenerating the detector library).
OPENROUTER_API_KEY=
